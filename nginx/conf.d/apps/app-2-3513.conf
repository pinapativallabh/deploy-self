# Auto-generated by Bonk — do not edit manually.
# Deployment: app-2-3513

upstream bonk_upstream_app_2_3513 {
    server bonk-7e6ae686-6bb5-4b9e-8304-6b59c50152fb-1:3000;
}

server {
    listen 80;
    server_name app-2-3513.localhost.nip.io;

    # Proxy all traffic to the deployed application container.
    location / {
        proxy_pass http://bonk_upstream_app_2_3513;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Websocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Sensible timeouts for long-running requests / SSE
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}

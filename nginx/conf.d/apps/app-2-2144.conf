# Auto-generated by Bonk — do not edit manually.
# Deployment: app-2-2144

upstream bonk_upstream_app_2_2144 {
    server bonk-4d5cb5e0-6e61-46fd-809e-ce0e3a38527a-1:3000;
}

server {
    listen 80;
    server_name app-2-2144.localhost.nip.io;

    # Proxy all traffic to the deployed application container.
    location / {
        proxy_pass http://bonk_upstream_app_2_2144;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Websocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Sensible timeouts for long-running requests / SSE
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}

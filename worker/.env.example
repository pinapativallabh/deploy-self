# General Application Settings
WORKER_NAME="ForgeDeploy Worker"
APP_ENV=development
LOG_LEVEL=info

# Redis Settings (Declared for future use, not currently wired into worker logic)
REDIS_HOST=redis
REDIS_PORT=6379

# Docker Engine (Declared for future use, not currently wired into worker logic)
DOCKER_HOST=unix:///var/run/docker.sock

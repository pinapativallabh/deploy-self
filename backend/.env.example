# General Application Settings
APP_NAME="ForgeDeploy Backend"
APP_ENV=development
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Database Settings (For future use, not currently wired into app logic)
POSTGRES_USER=forgeadmin
POSTGRES_PASSWORD=forgepassword
POSTGRES_DB=forgedeploy
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis Settings (For future use, not currently wired into app logic)
REDIS_HOST=redis
REDIS_PORT=6379
